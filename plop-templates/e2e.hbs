import { {{~browser}}, {{upperCase browser}}Browser, Page} from 'playwright'

describe('{{upperCase name}}', () => {
  let browser: {{upperCase browser}}Browser
  let page: Page

  beforeAll(async () => {
    browser = await {{browser}}.launch()
  })
  afterAll(async () => {
    await browser.close()
  })

  beforeEach(async () => {
    page = await browser.newPage()
  })
  afterEach(async () => {
    await page.close()
  })

  it('should work', async () => {
    await page.goto('http://localhost:3000/')
    expect(await page.title()).toBe('CNA | TypeScript')
  })
})
